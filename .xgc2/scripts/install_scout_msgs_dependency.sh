#!/usr/bin/env bash
# Retired. Packaging must not apt-get.
# Host: fetch_xgc2_runtime_debs.sh
# Image: install_local_xgc2_debs.sh (dpkg -i only)
echo "install_scout_msgs_dependency.sh is retired; do not apt-get inside the build image" >&2
exit 1
