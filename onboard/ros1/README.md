# Onboard ROS1 workspace

Catkin packages copied from the Xavier `/home/agilex` tree.

```text
src/imu/                 serial_imu
src/chassis/             wrp_io, ugv_sdk, scout_base
                         (scout_msgs and scout_description come from APT)
src/communication/       official swarm_ros_bridge from APT
src/autostart/           systemd, udev, compose launches
```
